public class ProviderController {

    // UNCOMMENT WHEN DATA CLASS IMPLEMENTED
    // private ProviderData providerList[];

    public void addProvider() {
        // Be called when user selects to add user
        // Prompt user to input: name, street, city, state, ZIP, and
        // provider number.
        // Generate new ProviderData object which stores that info
        // Display info on screen to have user validate
        // Store ProviderData object in providerList[]
        // Terminate
    }

    public void deleteProvider() {
        // Prompt user for provider number
        // Iterate through providerList until corresponding object
        // found
        // Remove object from list
        // Terminate
    }

    public void updateProvider() {
        // Prompt user for provider number
        // Iterate through providerList until corresponding object
        // found
        // Display information on screen
        // Prompt user for what information they seek to modify
        // Take user input
        // Modify object
        // Display information for validation
        // Prompt user for modification
        // Terminate if none
    }

    public void validateProvider() {
        // Iterate through providerList until object with provider
        // number found
        // Return true if found
        // Return false if not
        // Terminate
    }
}
